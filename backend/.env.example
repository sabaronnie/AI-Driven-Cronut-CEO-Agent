# OpenClaw (Claude Code) CLI command
# Default is "claude" — change if your installation uses a different path
# Windows example: C:\Users\YourName\AppData\Roaming\npm\claude.cmd
CLAUDE_CMD=claude

# Anthropic API key (required for Docker, optional if Claude Code is already authenticated)
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Server port (default: 8000)
PORT=8000
